# Add Cloudflare API Token needed by traefik

TODO: Make this a sealed secret and check into git.

```bash
kubectl -n traefik create secret generic cloudflare-api-token --from-literal apiToken=<API_KEY_GOES_HERE>
```
